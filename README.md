# Personal Site (GitHub Pages-ready)

## What this is
A static landing page with a clean dark theme.

## Local preview
Open `index.html` in a browser.

## Publish to GitHub Pages
1. Create repo named `<username>.github.io` (or any repo + Pages settings).
2. Push contents of this folder to the repo root.
3. In GitHub: Settings → Pages → Deploy from branch (`main` / root).

## Next customizations
- Replace placeholder text in sections
- Add project links/images
- Add social/contact links
- Optional: migrate to Jekyll for blog posts
